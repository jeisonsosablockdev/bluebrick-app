{
  "mcpServers": {
    "solana-mcp-server": {
      "type": "http",
      "url": "https://mcp.solana.com/mcp"
    },
    "helius": {
      "command": "bash",
      "args": ["-lc", "set -a; [ -f .env.local ] && . ./.env.local; set +a; exec npx helius-mcp@latest"]
    }
  }
}
