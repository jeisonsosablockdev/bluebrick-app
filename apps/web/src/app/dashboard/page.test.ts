// Just to have a structural test before changing page.tsx
import { describe, it, expect } from "vitest";

describe("DashboardPage", () => {
  it("should not hardcode user_sofia_martinez and fetch the authenticated investor", () => {
    expect(true).toBe(true);
  });
});
